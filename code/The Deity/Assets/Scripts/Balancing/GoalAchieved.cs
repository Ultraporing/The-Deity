using Assets.Scripts.Environment.Planet;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoalAchieved : MonoBehaviour {

	// Use this for initialization
	void Start () {
        

    }
	
	// Update is called once per frame
	void Update () {
		
        /*if(PlanetDatalayer.Instance.GetManager<GoalManager>().m_Houses == false && PlanetDatalayer.Instance.GetManager<GoalManager>().m_CycleNumber == 2)
        {
            PlanetDatalayer.Instance.GetManager<GoalManager>().m_Houses = true;
            PlanetDatalayer.Instance.GetManager<GoalManager>().m_SomethingChanged = true;
        }*/
	}
}
